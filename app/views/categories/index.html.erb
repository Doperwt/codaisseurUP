<h4>Themes</h4>
<% @categories.each do |category| %>
  <span class="label label-success"><%= link_to category.name, category_path(category.id) %></span>
<% end %>
<%= link_to "New category", new_category_path, class:"btn btn-danger"%>
