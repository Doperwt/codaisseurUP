<h4>Themes</h4>
<% @categories.each do |category| %>
  <span class="btn btn-default"><%= link_to category.name, category_path(category.id) %></span>
<% end %>
<br><br>
<br>

<%= link_to "New category", new_category_path, class:"btn btn-danger"%>
